list = []
# print(help(list)) prints all the methods and functions

set = {"apple"}
#have same functions as list, immutable, No duplicates unlike list, can add/remove

tuple = ("apple")
#have same functions, immutable, duplicates alolowed, FASTER, can't add/remove
print(len(list))